# --- Database Configuration ---
# Individual connection parameters (used by ui/src/lib/server/db.ts)
PGHOST=localhost
PGPORT=5432
PGDATABASE=worshipos
PGUSER=worship
PGPASSWORD=worship

# Full connection string (used by scripts/db-migrate.sh)
DATABASE_URL=postgres://worship:worship@localhost:5432/worshipos

# --- Frontend / UI Configuration ---
# Base URL for API calls (used by ui/src/lib/api.ts)
# Leave empty if the API is served from the same origin as the UI
VITE_API_BASE_URL=

# --- SvelteKit / Production Configuration ---
# The port the application runs on
PORT=5173
# The origin of the application (required for CSRF protection in production)
# ORIGIN=https://your-domain.com
