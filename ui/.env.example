# Database Configuration (Server-side)
PGHOST=localhost
PGPORT=5432
PGDATABASE=worshipos
PGUSER=worship
PGPASSWORD=worship

# Frontend / UI Configuration (Client-side)
# Prefixed with VITE_ so they are accessible in the browser
VITE_API_BASE_URL=
